import {makeStyles} from "@material-ui/core/styles";

export const landingFooterStyle = makeStyles( theme => ({
    root:{
        backgroundColor: theme.palette.primary.dark,
        padding: theme.spacing(6, 0)
    }
}));